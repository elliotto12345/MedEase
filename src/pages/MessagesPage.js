import Sidebar from "../components/Sidebar";

const MessagesPage = () => {
  return (
    <div style={{ display: "flex" }}>
      <Sidebar />
      Messages
    </div>
  );
};

export default MessagesPage;
