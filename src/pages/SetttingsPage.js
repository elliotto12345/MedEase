import Sidebar from "../components/Sidebar";

const SettingsPage = () => {
  return (
    <div style={{ display: "flex" }}>
      <Sidebar />
      Settings
    </div>
  );
};

export default SettingsPage;
